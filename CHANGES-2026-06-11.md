# Implementation Manifest — 2026-06-11 rework

Companion to `WAVE-REWORK-2026-06-11.md` (schedule) + `COPY-REWORK-2026-06-11.md` (copy
rationale). This file = exactly what changes, where each change lives in git, and how it
was verified. Work branch: **`rework-2026-06-11`** (off main) → merged to main; branch-side
commits cherry-picked along the wave chain.

## Survey corrections (vs the worklist's assumptions)
- **Dead pages live on MAIN**, not wave branches — the 16 expansion pages landed on main in
  `lib/verticals/*-answering-service.ts` + `lib/verticals/expansion.ts`. Wave branches hold the
  *scheduled* wave pages (dental w2 · 24-7/cheap/vs-hiring w3 · bilingual/live-answering w4 ·
  RE/PM/salon/vet w5 · medical/ai-receptionist/missed-call w6 (`12a9bcf`) · spam w8 · plumbing w9 (`1c28f9a`)).
  Branches are one stacked chain (w2⊂w3⊂w4⊂w5⊂w6⊂w8⊂w9) + 2 instrumentation commits per tip.
- Branch vertical configs are already job-free in titles; the job problem is on main.

## C1 — Category homepage (main)
- NEW `components/CategoryHomePage.tsx` — homepage-specific design reusing the site's design
  system (la-hero / trust-strip / oneliner / stakes / plan / pricing / faq / final-cta classes,
  HeroCallForm + HeroLogoVideo), plus the homepage-only **verticals router grid**.
- `app/page.tsx` — renders CategoryHomePage; title
  `AI Receptionist & 24/7 Answering Service for California | Live Answer`; canonical `/`.
- `lib/verticals/hvac.ts` — canonical `/` → `/services/hvac-answering-service` (kills the
  homepage/HVAC duplication; HVAC lives only on its service page).
- Grid links only Wave-1-promoted services (hvac, attorney, small-business) + `/services`;
  grow it as waves promote.

## C2 — Job-language ban (main; titles/meta/H1 strict, body thinned)
| File | Change |
|---|---|
| `lib/verticals/hvac.ts` | meta title `…Never lose another job` → `…Never miss another call` |
| `lib/services-data.ts` | h1 `Never Lose Another HVAC Job to Voicemail` → `…HVAC Call to Voicemail` |
| `lib/verticals/live-chat-answering-service.ts` | `book jobs` → `book customers`; h1 `…Booked Job` → `…Booked Appointment` |
| `lib/verticals/outbound-reminder-answering-service.ts` | `booked the job` ×2 → `booked the appointment` |
| `components/VerticalLandingPage.tsx` | `lose the job` → `lose the customer` |
| `app/calculator/page.tsx` | visible strings: `Average job value` → `Average ticket value`; `missed jobs ×` → `missed calls ×` (internal var names untouched) |
| `app/how-it-works/page.tsx` | `competitor's job` → `competitor's customer` |
| `app/free-trial/page.tsx` | `while you're on a job` → `while you're on a service call` |
| `app/locations/[slug]/page.tsx` | "Job stories" heading → "Stories from the field" (data-driven content untouched) |
- Replacement language = the higher-volume search vocabulary (call/customer; `missed call` 12,100/mo).
- Target: ≤30 visible "job" mentions repo-wide on main (was 112); roofing/towing removal (C4) helps.

## C3 — Aim fixes (main)
- `lib/verticals/appointment-scheduling-answering-service.ts` → retarget to **AI appointment
  setter** (320/mo · KD 7): title/h1/meta lead with the exact phrase.
- `lib/verticals/insurance-answering-service.ts` — left in place but NOT scheduled (excluded in
  site-plan; superseded by the future `/services/insurance-verification` build).

## C4 — Dead-page removal (main)
Delete 6 expansion pages (≤20–50/mo, flat/declining; CNI risk):
`accounting`, `cleaning-landscaping`, `it-msp`, `nonprofit`, `roofing-construction`,
`towing-roadside` (-answering-service.ts) + their imports/entries in `expansion.ts` and refs in
`vertical-tints.ts`, `service-faqs.ts`, `gbp-categories-data.ts`, `app/dev-nav/page.tsx`.
(Wave branches never had them — no branch-side removal needed.)

## Branch ops (the new schedule's code moves)
| Op | Branch | Commit |
|---|---|---|
| Cherry-pick wave-6 trio `12a9bcf` (medical vertical + /ai-receptionist + /missed-call-recovery) | → wave-3 | brings /ai-receptionist for its new Wave-3 slot; medical+missed-call ride along noindexed (safe per gating rules) |
| Medical retarget — `lib/verticals/medical.ts` title/h1/intro to exact phrase **"medical answer service"** (6,600/mo · KD 7; the "-ing" form is 0/mo) | commit on wave-3, cherry-pick → wave-6, wave-8, wave-9 | identical content on every branch ⇒ clean publish-Monday merges |
| Cherry-pick plumbing `1c28f9a` | → wave-5 | plumbing's new Wave-5 slot |

## New builds (NOT in this change — next dev pass, per WAVE-REWORK)
`/ai-answering-service` (W3) · `/services/funeral-home-answering-service` (W4) ·
`/services/insurance-verification` (W5) · `/services/prior-authorization-service` (W6).

## Verification
- [ ] `npm run build` green on: main, wave-3, wave-5
- [ ] visible "job" count ≤30 on main (`grep -rhoiE '\bjobs?\b'` on copy files)
- [ ] dead slugs resolve to 404 intent: removed from registry/sitemap/dev-nav
- [ ] /services/hvac-answering-service canonical self-referencing; homepage canonical `/`
- [ ] wave-3 has /ai-receptionist; wave-5 has plumbing; medical retarget identical on w3/w6/w8/w9
- [ ] POST-DEPLOY: re-run `fetch_labs.py site` (keywords_for_site) — success = content read flips
      from job-seeker queries (was 88/96) to buyer queries
